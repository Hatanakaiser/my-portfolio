# Gitにテキストファイルを自動判別させ、
# リポジトリ（Gitのデータベース）に保存する際はLFに統一する
* text=auto eol=lf

# 明示的にLFを維持したいファイル
*.js    text eol=lf
*.jsx   text eol=lf
*.ts    text eol=lf
*.tsx   text eol=lf
*.css   text eol=lf
*.html  text eol=lf
*.json  text eol=lf
*.md    text eol=lf

# バイナリファイル (改行コードを変換させない)
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary